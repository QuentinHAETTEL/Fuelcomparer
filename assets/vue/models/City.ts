export default class City {
    name: string = '';
    postcode: string = '';


    constructor(name: string, postcode: string)
    {
        this.name = name;
        this.postcode = postcode;
    }
}